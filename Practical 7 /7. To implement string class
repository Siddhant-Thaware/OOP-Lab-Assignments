#include <iostream>
#include <cstring>
using namespace std;

class String
{
    char *str;

public:
    String()
    {
        str = new char[1];
        str[0] = '\0';
    }

    String(const char *s)
    {
        str = new char[strlen(s) + 1];
        strcpy(str, s);
    }

    ~String()
    {
        delete[] str;
    }

    void accept()
    {
        char temp[100];

        cout << "Enter a string: ";
        cin >> temp;

        delete[] str;

        str = new char[strlen(temp) + 1];
        strcpy(str, temp);
    }

    void display()
   
